<!-- rounded_background.xml -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/cardview_light_background" /> <!-- Colore di sfondo -->
            <corners android:radius="8dp" /> <!-- Radius dei bordi -->
        </shape>
    </item>
    <item android:top="1dp" android:right="1dp" android:left="1dp" android:bottom="3dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/cardview_light_background" /> <!-- Colore dell'ombra -->
            <corners android:radius="8dp" /> <!-- Radius dei bordi -->
        </shape>
    </item>
</layer-list>