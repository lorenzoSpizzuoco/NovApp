<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item android:right="0dp" android:top="0dp" android:left="0dp" android:bottom="0dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/info_chip_background" />
        </shape>
    </item>

    <!-- Parte sinistra con colore bianco (90% della larghezza) -->
    <item android:right="10dp" android:left="0dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/background_white" />
        </shape>
    </item>
</layer-list>
